<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.adapter.example">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GridLayoutManagerActivity" />
        <activity android:name=".activity.LinearLayoutManagerActivity" />
        <activity android:name=".activity.StaggeredGridLayoutManagerActivity" />
        <activity android:name=".net.NetWorkActivity" />
        <activity android:name=".activity.CollapsingToolbarLayoutActivity" />
        <activity android:name=".activity.EmptyViewActivity" />
        <activity android:name=".activity.MultipleXXItemActivity" />
        <activity android:name=".activity.DataBindingActivity" />
    </application>

</manifest>